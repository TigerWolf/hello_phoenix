<h2>New log</h2>

<%= render "form.html", changeset: @changeset, activities:  @activities,
                        action: log_path(@conn, :create), conn: @conn %>

<%= link "Back", to: log_path(@conn, :index) %>
